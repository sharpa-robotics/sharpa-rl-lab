[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sharpa_tac_rl"
version = "0.1.0"
description = "sharpa rl with tactile"
authors = [{ name = "renrenyuan", email = "renyuan.ren@sharpa.com" }]
dependencies = []
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["rl_isaaclab*"]
